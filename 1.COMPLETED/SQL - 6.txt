1)	Display the details all Managers of Accounting Department.

SELECT * FROM EMP
WHERE JOB = 'MANAGER' AND DEPTNO = 
(SELECT DEPTNO FROM DEPT
WHERE DNAME='ACCOUNTING');

2)	Display all the Salesman of sales dept who are receiving some commision

SELECT * 
FROM EMP
WHERE JOB='SALESMAN' AND COMM IS NOT NULL;

3)	Display all managers and analysts of both Accounting and Reasearch Department who annual salary is ranging from 15000 to 40000 
        in the descending order of sal.

SELECT *
FROM EMP WHERE JOB IN ('MANAGER', 'ANALYST') AND DEPTNO IN ( SELECT DEPTNO
FROM DEPT 
WHERE DNAME IN ('ACCOUNTING', 'RESEARCH'))
ORDER BY SAL DESC;



4) 	Display all 1981 and 1982 employees joined in Newyork and Chicago with salary morethan 1250 joined under some manager.  

SELECT * FROM EMP, DEPT
WHERE EMP.DEPTNO = DEPT.DEPTNO 
AND TO_CHAR(EMP.HIREDATE, 'YYYY') IN (1981, 1982) 
AND DEPT.LOC IN ('NEW YORK', 'CHICAGO')
AND EMP.SAL > 1250 
AND EMP.MGR IS NOT NULL;



5)	 Display grade of Adams.

SELECT GRADE
FROM SAL_GRADE
WHERE (SELECT SAL FROM EMP WHERE ENAME='ADAMS') BETWEEN 
LOSAL AND HISAL;



6) 	Disaply all grade 2 Salesman.

SELECT * FROM EMP
WHERE JOB = 'SALESMAN' AND
SAL BETWEEN (SELECT LOSAL FROM SAL_GRADE WHERE GRADE=2)
AND (SELECT HISAL FROM SAL_GRADE WHERE GRADE = 2);

7) 	Display all grade 4 & 5 managers working under some manager having an experience not less thean 35 years.

SELECT * FROM EMP E
WHERE JOB = 'MANAGER' AND
SAL BETWEEN (SELECT LOSAL FROM SAL_GRADE WHERE GRADE=4)
AND (SELECT HISAL FROM SAL_GRADE WHERE GRADE = 5)
AND MGR IS NOT NULL AND 
(SELECT MONTHS_BETWEEN(SYSDATE,E1.HIREDATE)/12 FROM EMP E1 WHERE
E1.EMPNO = E.MGR) >=35 ;

OR 

SELECT * FROM EMP E
WHERE EMPNO IN (SELECT MGR FROM EMP WHERE MGR IS NOT NULL) AND
SAL BETWEEN (SELECT LOSAL FROM SAL_GRADE WHERE GRADE=4)
AND (SELECT HISAL FROM SAL_GRADE WHERE GRADE = 5)
AND MGR IS NOT NULL AND 
(SELECT MONTHS_BETWEEN(SYSDATE,E1.HIREDATE)/12 FROM EMP E1 WHERE
E1.EMPNO = E.EMPNO) >=35 ;



8) 	Display the details of employees belongs to New York working as Managers beloging to grade 4 & 5.


SELECT * FROM EMP, DEPT
WHERE EMP.DEPTNO = DEPT.DEPTNO AND DEPT.LOC = 'NEW YORK' AND 
JOB = 'MANAGER' AND SAL BETWEEN 
(SELECT LOSAL FROM SAL_GRADE WHERE GRADE = 4) AND 
(SELECT HISAL FROM SAL_GRADE WHERE GRADE = 5);